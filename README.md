# HobbyFlow Server

Node.js API for AI-assisted hobby learning plans.

## Scripts

- `npm run dev` — start dev server with hot reload
- `npm test` — run tests
- `npm run build` — compile TypeScript

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

Health check: `GET http://localhost:3000/health`
